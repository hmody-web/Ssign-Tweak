# Ssign Tweak

GitHub-ready Theos project for building `SsignTheme.dylib`.

## Build
Open **Actions** → **Build Ssign Tweak** → **Run workflow**.

When the build succeeds, download the artifact named:

`SsignTheme-dylib`

Then inject `SsignTheme.dylib` into the IPA and re-sign it.
